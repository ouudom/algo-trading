# ============================================================
# AlgoTrader Backend — Production Dependencies
# Python 3.12+
# ============================================================

# --- Data Science & Numerics ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0            # Parquet engine

# --- Technical Analysis ---
# TA-Lib requires system lib: brew install ta-lib  /  apt install ta-lib
TA-Lib>=0.4.28
pandas-ta>=0.3.14b       # Pure-Python fallback indicators
vectorbt>=0.26           # Vectorised backtesting utilities

# --- MetaTrader 5 ---
# MetaTrader5>=5.0.45   # native Windows only — install manually on Windows
mt5linux>=0.1.3          # Linux bridge via Wine socket (used when MetaTrader5 is not importable)

# --- Database ---
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29            # Async PostgreSQL driver
psycopg2-binary>=2.9     # Sync driver (used by Alembic migration runner)
greenlet>=3.0            # Required by SQLAlchemy async (greenlet_spawn)

# --- API ---
fastapi>=0.111
uvicorn[standard]>=0.29  # ASGI server with websockets + reload support
httpx>=0.27              # Async HTTP client (also used in tests)
python-multipart>=0.0.9  # Required by FastAPI for UploadFile / form data

# --- Pydantic / Settings ---
pydantic>=2.7
pydantic-settings>=2.2

# --- Scheduling ---
APScheduler>=3.10        # Cron-style scheduling for data feed / strategy runner

# --- Scientific ---
scipy>=1.13

# --- Utilities ---
python-dotenv>=1.0
