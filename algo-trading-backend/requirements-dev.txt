# ============================================================
# AlgoTrader Backend — Development / CI Dependencies
# Install with: pip install -r requirements-dev.txt
# ============================================================

-r requirements.txt

# --- Testing ---
pytest>=8.2
pytest-asyncio>=0.23     # async test support
httpx>=0.27              # HTTPX AsyncClient for FastAPI test client
aiosqlite>=0.20          # In-memory SQLite for DB tests (no PostgreSQL needed in CI)

# --- Linting & Formatting ---
ruff>=0.4                # Fast linter + import sorter
black>=24.4              # Opinionated code formatter

# --- Type Checking (optional) ---
mypy>=1.10
types-python-dateutil>=2.9
